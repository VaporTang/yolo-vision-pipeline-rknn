# Shared Windows dependencies for both the training and export Conda environments.
# PyTorch is installed separately because the project uses the CUDA 12.4 wheel index.
# Ultralytics is installed separately by setup_win.ps1.
opencv-python==4.8.1.78
numpy==1.24.3
pyyaml==6.0
pillow==10.0.0
requests==2.31.0
tqdm==4.66.1
matplotlib==3.7.2
scikit-learn==1.3.1
pydantic==2.3.0
onnx==1.14.1
onnxsim==0.4.35
PySide6==6.6.1
